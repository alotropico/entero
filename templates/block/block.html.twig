{#
/**
 * @file
 * Template for a block.
 */
#}
{% set newclass = elements['#configuration'].label|length? elements['#configuration'].label : 'no-label' %}

{% include '@radix/block/block.twig' with {
  html_tag: 'div',
  utility_classes: ['block--' ~ newclass|replace({' ': ''})|lower],
} %}
