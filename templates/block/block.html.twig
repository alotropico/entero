{#
/**
 * @file
 * Template for a block.
 */
#}

{% include '@radix/block/block.twig' with {
  html_tag: 'div',
  utility_classes: ['block--' ~ elements['#configuration'].label|replace({' ': ''})|lower],
} %}
